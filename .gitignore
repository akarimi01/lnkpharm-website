# OS / editor junk
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/

# Logs
*.log
npm-debug.log*

# Build artifacts (none expected for a static site, but future-proof)
dist/
build/
.cache/

# Environment files (we don't have any, but never commit if added)
.env
.env.local
.env.*.local
